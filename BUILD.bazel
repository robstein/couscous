load("@bazel_gazelle//:def.bzl", "gazelle")
load("@bazel_buildtools//buildifier:def.bzl", "buildifier")

gazelle(
    name = "gazelle",
    prefix = "github.com/robstein/orabo",
)

buildifier(
    name = "buildifier",
)